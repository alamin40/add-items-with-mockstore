/*

import { initializeApp } from './main';
import { mockStore } from '../tests/mockStore'; // Import the mock store

// Use the mock store for testing or debugging
initializeApp(mockStore()).mount('#app');

*/